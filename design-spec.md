# AI 伴侣 App UI 重设计 · 设计 Spec

## 产品概述
**「TA」** 是一款 Android 原生 AI 伴侣应用（包名 com.agent.ta）。用户与一个可深度定制的 AI Agent 进行日常聊天，Agent 有情绪状态机（开心/工作/睡觉/洗澡/游戏/无聊），根据状态自动切换语气、头像和回复延迟。支持 LLM + TTS 语音回复，类似微信的语音消息体验。

## 目标受众与使用场景
- **受众**：个人用户，寻求情感陪伴和日常对话
- **场景**：日常聊天、情感倾诉、打发时间；用户发送文字/emoji，Agent 回复文字+语音
- **使用频率**：高频，每天多次打开

## 核心信息与内容要点（6 个核心页面）

### 1. 聊天页（ChatScreen）— 最核心
- 顶部：Agent 名称 + 当前状态指示（如"工作中"）
- 中间：消息流（用户气泡靠右、Agent 气泡靠左）
  - 文字消息：气泡 + 文字
  - 语音消息：语音条（点击播放，WeChat 风格）
  - emoji 消息：大字号无气泡显示
  - Agent 消息可带动作描述（如"(笑了笑)"）
- 底部：输入框 + 发送按钮 + 语音按钮
- Agent 状态影响：睡觉/洗澡时不显示"正在输入中"

### 2. 设置页（ProfileScreen）
- 顶部：返回栏 + "设置"标题
- 头像资料卡：圆形头像 + Agent 名称 + 年龄 + 当前状态 chip
- 分组菜单（4组）：
  - 账户：用户称呼
  - Agent：今日作息、Agent 配置
  - 设置：模型配置（LLM/TTS）
  - 危险操作：重置聊天记录、重置记忆

### 3. Agent 配置入口（AgentConfigScreen）
- Agent 信息卡：头像 + 名称 + "使用中"徽章 + 基本信息
- 导入/导出按钮
- 配置入口列表（5项）：基础信息、人格设定、头像管理、语音配置、行为配置

### 4. 人格设定（AgentPersonaScreen）
- 性格与口头禅：性格标签 + 口头缀词
- 说话风格：语调、用词层级、语速、句长（各一排滑块/选择器）
- 兴趣与禁忌：兴趣话题、禁忌话题
- 初始共享记忆
- 关系阶段提示
- 示例对话

### 5. 模型配置（ModelConfigScreen）
- 用户称呼设置
- LLM 模型配置：模型选择器 + baseUrl + API Key + 模型名
- TTS 模型配置：模型选择器 + baseUrl + API Key + 模型名

### 6. 头像管理（AgentAvatarScreen）
- 头像列表（每个头像一张卡片）：头像图片 + 绑定状态/情绪 + 触发关键词
- 浮动添加按钮
- 底部保存按钮

## 情感基调与气质关键词
- **温暖**：像真人朋友，不是工具
- **柔软**：界面不刺眼，有呼吸感
- **有温度**：色彩温暖，不冷冰冰
- **沉浸**：聊天时设计退隐，内容为主角
- **活泼但不喧闹**：Agent 有情绪变化，但不花哨

## 输出格式与尺寸
- **格式**：单文件 HTML（inline React + Babel），`file://` 双击可开
- **设备框**：使用 AndroidFrame（Pixel 风格，412×892px）
- **布局**：平铺 4-6 台 Android 设备，每台展示一个核心页面
- **交互**：每台独立可交互（tab 可切、按钮可点、能弹 modal）
- **图片**：头像用渐变占位（CSS 渐变），不需要真实图片

## 已知约束
- **品牌色**：teal #1B5E5C（必须保留为主色）
- **品牌色辅助**：teal-500 #14B8A6（渐变起点）、teal-100 #CCFBF1（柔和底）
- **平台**：Android Material 3 风格，但设计探索不限于 Material
- **字体**：中文优先，系统字栈即可
- **禁忌**：不要 Material Design 3 默认紫色、不要 AI slop（紫渐变、emoji 图标、圆角卡片+左 border accent）

## 视觉母题
这个内容独有的视觉元素/结构/隐喻：
1. **气泡**：对话是核心，气泡形状/色彩定义页面气质
2. **状态机**：Agent 有 6 种情绪状态，状态切换是独有交互——可以用颜色/渐变映射状态
3. **呼吸感**：Agent 像活物，界面应有微妙的呼吸/脉动感（不是死板的静态卡片）
4. **声波**：语音消息是核心功能，声波可视化是独有视觉元素

## 三套逻辑分配

### 逻辑一 · 🎲 像素游戏横版叙事（轮盘 9 号）
- **风格 DNA**：分关卡换色（Agent 状态 = 关卡）、游戏化 HUD、视差分层、像素美学
- **迁移到 AI 伴侣**：
  - Agent 状态切换 = 换"关卡"色调（开心=暖橙、工作=冷蓝、睡觉=深紫、洗澡=青绿、游戏=电光蓝、无聊=灰）
  - 聊天页面 = 横版叙事场景，消息流有轻微视差
  - 头像 = 像素角色感（用 CSS 几何拼）
  - 顶部状态栏 = 游戏 HUD（HP 条 = 亲密度、状态图标）
- **还原度**：70%（像素角色用 CSS 几何降级，不假装手绘像素插画）
- **字体**：Press Start 2P / VT323（仅标题/标签）+ Inter（正文）

### 逻辑二 · 🏆 Pi by Inflection 现实参照
- **参照案例**：Pi (heypi.com) — AI 伴侣应用设计标杆
- **设计语言**：温暖柔和、大量留白、圆润字体、单色系但有温度、对话为中心
- **迁移要点**：
  - 聊天页是对话主角，UI 极度退隐
  - 柔和的奶油色/暖白底，teal 作为唯一 accent
  - 圆润的气泡形状，不要锐角
  - 状态指示用柔和的色彩渐变，不是生硬的标签
  - 设置/配置页也保持温暖，不是冷冰冰的列表
- **字体**：Söhne / Inter（圆润无衬线）+ 可选衬线 display

### 逻辑三 · 🧠 原研哉设计哲学
- **设计师**：原研哉（无印良品艺术总监）
- **设计哲学**：「白」、「空」、「再设计」——让已有事物重新被发现
- **迁移要点**：
  - 极致留白，留白是构图不是空缺
  - teal 仅在关键点出现（发送按钮、选中态），其余全是黑/白/灰
  - 文字排印为主角，字号/字重做层级，不用色块分区
  - 气泡几乎没有背景色，用细线或空间区分
  - 配置页像翻书，安静的表单
  - 整体气质：安静、克制、有呼吸、不喧闹
- **字体**：Noto Serif SC（衬线 display）+ Noto Sans SC（正文）
